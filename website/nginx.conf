events {}
http {
    server {
        listen 80;
        location / {
            root /app;
            try_files $uri $uri/ =404;
        }
        location /api/ {
            proxy_pass http://127.0.0.1:5000/;
        }
    }
}
